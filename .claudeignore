# Prevent Claude Code from reading config files that contain API keys/secrets
# Only config.yaml has secrets; other yaml files (models, routing, prompts, tags) are safe
web3-insight/backend/config/config.yaml
